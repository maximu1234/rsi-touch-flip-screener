{
  "name": "rsi-touch-flip-screener",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone RSI Touch Flip universe fit (Bybit linear). Not part of Multichart.",
  "scripts": {
    "start": "node src/server.js",
    "scan": "node src/cli.js",
    "test": "node --test tests/*.test.mjs",
    "sync:web": "cp lib/rsi-touch-flip-*.js lib/suitability-score.js lib/screener-defaults.js lib/import-results.js public/lib/"
  },
  "engines": {
    "node": ">=20"
  }
}
